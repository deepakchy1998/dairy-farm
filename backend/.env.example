PORT=5000
MONGODB_URI=mongodb+srv://user:pass@cluster.mongodb.net/dairypro
JWT_SECRET=your-super-secret-jwt-key-change-this
GEMINI_API_KEY=your-gemini-api-key

# Razorpay (for online payments — UPI, cards, wallets, netbanking, QR)
# Get keys from https://dashboard.razorpay.com → Settings → API Keys
# Use rzp_live_xxx for production, rzp_test_xxx for testing
RAZORPAY_KEY_ID=rzp_live_xxxxxxxxxxxx
RAZORPAY_KEY_SECRET=your-razorpay-key-secret
# Webhook secret: Dashboard → Webhooks → Create → copy secret
# Webhook URL: https://your-api-domain.com/api/razorpay/webhook
# Events to subscribe: payment.captured, payment.failed, order.paid
RAZORPAY_WEBHOOK_SECRET=your-webhook-secret

# WhatsApp Cloud API (free — 1000 conversations/month)
# Get these from https://developers.facebook.com → WhatsApp → API Setup
WHATSAPP_TOKEN=your-whatsapp-permanent-token
WHATSAPP_PHONE_ID=your-phone-number-id
